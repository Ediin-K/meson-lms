# Backend
DB_HOST=localhost
DB_PORT=3306
DB_NAME=meson_lms
DB_USER=root
DB_PASSWORD=
JWT_SECRET=changeme-generate-your-own-base64-secret-do-not-reuse-the-repo-default
JWT_EXPIRATION=86400000

# Email (bulk-import temp-password notifications) - optional, off by default
MAIL_ENABLED=false
MAIL_FROM=no-reply@meson-lms.com
MAIL_HOST=smtp.sendgrid.net
MAIL_PORT=587
MAIL_USERNAME=apikey
MAIL_PASSWORD=

# Frontend
VITE_API_BASE_URL=http://localhost:8080/api
